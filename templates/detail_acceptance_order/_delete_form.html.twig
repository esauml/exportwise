<form method="post" action="{{ path('detail_acceptance_order_delete', {'id': detail_acceptance_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_acceptance_order.id) }}">
    <button class="btn">Delete</button>
</form>

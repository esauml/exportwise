{% extends 'base.html.twig' %}

{% block title %}DetailAcceptanceOrder index{% endblock %}

{% block body %}
    <h1>DetailAcceptanceOrder index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>Unit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for detail_acceptance_order in detail_acceptance_orders %}
            <tr>
                <td>{{ detail_acceptance_order.id }}</td>
                <td>{{ detail_acceptance_order.quantity }}</td>
                <td>{{ detail_acceptance_order.unit }}</td>
                <td>
                    <a href="{{ path('detail_acceptance_order_show', {'id': detail_acceptance_order.id}) }}">show</a>
                    <a href="{{ path('detail_acceptance_order_edit', {'id': detail_acceptance_order.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('detail_acceptance_order_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}DetailAcceptanceOrder{% endblock %}

{% block body %}
    <h1>DetailAcceptanceOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_acceptance_order.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ detail_acceptance_order.quantity }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ detail_acceptance_order.unit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_acceptance_order_index') }}">back to list</a>

    <a href="{{ path('detail_acceptance_order_edit', {'id': detail_acceptance_order.id}) }}">edit</a>

    {{ include('detail_acceptance_order/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}PurchaseOrder{% endblock %}

{% block body %}
    <h1>PurchaseOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purchase_order.id }}</td>
            </tr>
            <tr>
                <th>Date_done</th>
                <td>{{ purchase_order.dateDone ? purchase_order.dateDone|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ purchase_order.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('purchase_order_index') }}">back to list</a>

    <a href="{{ path('purchase_order_edit', {'id': purchase_order.id}) }}">edit</a>

    {{ include('purchase_order/_delete_form.html.twig') }}
{% endblock %}

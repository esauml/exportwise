<form method="post" action="{{ path('acceptance_order_delete', {'id': acceptance_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acceptance_order.id) }}">
    <button class="btn">Delete</button>
</form>
